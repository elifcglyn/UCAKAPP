import { DefaultTheme } from 'react-native-paper';

const theme = {
  ...DefaultTheme,
  colors: {
    ...DefaultTheme.colors,
    primary: '#DC143C',
    accent: '#FF6B6B',
    background: '#FFFFFF',
    surface: '#FFFFFF',
    text: '#1A1A1A',
    placeholder: '#999999',
    disabled: '#CCCCCC',
    backdrop: 'rgba(0, 0, 0, 0.5)',
  },
};

export default theme;

